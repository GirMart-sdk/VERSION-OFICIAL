{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "winner-store",
  "version": "2.0.0",
  "description": "🏆 WINNER STORE - Plataforma de e-commerce moderna para tiendas de ropa streetwear colombianas",
  "main": "backend/server.js",
  "type": "commonjs",
  "author": "GirMart-SDK",
  "license": "Proprietary",
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  },
  "scripts": {
    "prestart": "prisma generate",
    "start": "node backend/server.js",
    "dev": "pm2 startOrRestart ecosystem.config.js --name winner-store --no-daemon",
    "dev:stop": "pm2 delete winner-store || exit 0 && pm2 delete winner-store-backup-task || exit 0",
    "start:prod": "npm run setup && node backend/server.js",
    "start:local": ".\\INICIAR_TIENDA.bat",
    "setup": "npx prisma generate && npx prisma db push && node backend/seed.js",
    "build:frontend": "esbuild admin-bundle.js --bundle --minify --outfile=dist/bundle.js",
    "seed": "prisma db seed",
    "init": "npm install && npm run db:setup && npm run seed && npm run start:local",
    "init:silent": "npm install --silent && npm run db:setup && npm run seed --silent && npm run start:local",
    "test": "node -c backend/server.js && npm run audit",
    "audit": "npm audit --audit-level=high",
    "audit:fix": "npm audit fix",
    "lint": "eslint . --ext .js && echo '✅ Syntax check completado'",
    "security-scan": "eslint . --plugin security --rule 'security/detect-object-injection: warn' && echo '🛡️ Escaneo SAST completado'",
    "backup": "node scripts/db/backup.js",
    "wipe-data": "node scripts/db/wipe-data.js",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:generate": "prisma generate",
    "db:studio": "prisma studio",
    "db:check": "prisma db pull && echo '✅ Conexión con PostgreSQL establecida'",
    "db:setup": "node scripts/db/setup.js",
    "clean": "node scripts/utils/clean.js",
    "reinstall": "npm run clean && del package-lock.json && npm install",
    "reset": "npm run clean && npm run db:generate && npm run seed && echo '✅ Proyecto reseteado'",
    "logs": "type .env | findstr DB || echo 'Revisa backend/server.js para logs'",
    "postinstall": "prisma generate"
  },
  "keywords": [
    "ecommerce",
    "tienda-online",
    "streetwear",
    "colombia",
    "payment-gateway",
    "pse",
    "nequi",
    "daviplata"
  ],
  "dependencies": {
    "@prisma/adapter-pg": "^6.19.3",
    "@prisma/client": "^6.19.3",
    "axios": "^1.18.0",
    "body-parser": "^1.20.2",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "csurf": "^1.2.2",
    "dotenv": "^16.6.1",
    "express": "^4.19.2",
    "express-rate-limit": "^8.5.2",
    "helmet": "^7.2.0",
    "joi": "^17.13.3",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "nodemailer": "^9.0.1",
    "pdfkit": "^0.18.0",
    "pg": "^8.21.0",
    "pm2": "^7.0.1",
    "sharp": "^0.33.5",
    "winston": "^3.13.0",
    "winston-daily-rotate-file": "^5.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@prisma/config": "^6.19.3",
    "eslint": "^8.57.0",
    "eslint-plugin-security": "^2.1.1",
    "prisma": "^6.19.3",
    "watch": "^0.13.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/GirMart-sdk/VERSION-OFICIAL.git"
  },
  "bugs": {
    "url": "https://github.com/GirMart-sdk/VERSION-OFICIAL/issues"
  },
  "homepage": "https://github.com/GirMart-sdk/VERSION-OFICIAL#readme",
  "prettier": {}
}
